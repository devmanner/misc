# Desktop stuff
sudo apt install gnome-tweaks chrome-gnome-shell chromium-browser conky-all lm-sensors trash-cli

# Admin stuff
sudo apt install apt-file htop

# Dev stuff
sudo apt install vim erlang git vim-syntax-docker make docker.io curl

# Security stuff
sudo apt install nmap tor aircrack-ng forensics-extra geoip-bin testssl.sh wireshark

# Multimedia
sudo apt install vlc mplayer

# Extra openssl
sudo su -
cd /opt && \
mkdir openssl && \
cd openssl && \
wget  https://www.openssl.org/source/openssl-1.0.2q.tar.gz && \
tar xvfz openssl-1.0.2q.tar.gz && \
cd openssl-1.0.2q && \
./config --prefix=/opt/openssl shared enable-weak-ssl-ciphers && \
make && \
make install

